{
  "name": "arccopy",
  "version": "1.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "copy-manifest": "mkdirp dist && cp manifest.json dist/ && cp src/offscreen.html dist/ && cp assets/*.png dist/",
    "build": "esbuild src/background.ts src/offscreen.ts --bundle --outdir=dist --platform=browser --format=esm && pnpm run copy-manifest",
    "lint": "eslint \"src/**/*.ts\" \"tests/**/*.ts\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.18.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.304",
    "@typescript-eslint/eslint-plugin": "^8.24.0",
    "@typescript-eslint/parser": "^8.24.0",
    "esbuild": "^0.25.0",
    "eslint": "8.57.0",
    "mkdirp": "^3.0.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  },
  "packageManager": "pnpm@10.4.0"
}
